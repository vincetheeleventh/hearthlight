# Checkpoint — 2026-08-04

*Previous checkpoint: 2026-08-03*  
*Baseline commit: 155f7d43cd7cbdc9203c53518d1d088d22602981*

---

## What changed?

**cowork** (3 commits) — v1 defined and the North Star rewritten. `b0570fd` added the weekly
workshop, `PROPOSALS.md` (6 open, WIP 7, 30-day expiry), and the GREEN/AMBER/RED autonomy tiers
(D-013, D-014). `957e3d0` propagated Vince's `GOALS.md` rewrite across the canon: the feature test
went six criteria → four plus a prior question; `hearthlight-dashboard` reclassified UNCLEAR → CORE.
`34e0a87` corrected the pytest note.

**chatgpt** (1 commit, `532c191`) — recorded **Hearthlight Studio**: the UI is no longer read-only.
`PRODUCT_SPEC.md` §Features and `ROADMAP.md` §Gaps now describe comments, direct prompt editing,
generation queueing, stage approval, hero restore, bulk approve, and shot insert/retire/restore.
Only automatic critique-to-prompt rewriting remains unbuilt.

**UNATTRIBUTED — the day's largest body of work, none of it committed.** The Stage-A Krea image
pipeline was rebuilt:

- `krea_style_comp.py` +189 lines — sole Stage-A compiler; blocks stale registry/workbook hashes,
  unstable Shot IDs, shared-setup duplicates, source-photo jobs, forbidden motion text, and any
  packet differing from its `Still (frame one)` cell. Emits immutable packets + `batch-plan.json`.
- **New:** `krea_style_comp_run.py` — sole paid runner, consumes packets, never writes a prompt.
  Request fingerprints skip completed work; submitted-but-unfinished jobs resume by Krea job ID.
- `hearthlight-image-prompts/SKILL.md` and `hearthlight-shot-runner/SKILL.md` rewritten to the
  two-command compile/run contract, with a two-shot calibration gate before `--all`.
- `SHOT-IDENTITY-PROTOCOL.md` gained `beat`, `charge`, `motifs`, `never` — a `never` violation is a
  spec FAIL regardless of beauty.
- `selfcheck.py` repointed off the retired `image_pass.py` preflight.
- `staging/overview-ui/` appeared — 1.4 MB, 8 files, including `ui_server.py` (355 KB) and
  `productions.js` (82 KB). This is Hearthlight Studio. Untracked.

## Why does it matter?

`GOALS.md` §Current position puts v1 at image generation — style composition → likeness → final
input image. **Today's uncommitted work is exactly that stage**, and it executes `PROPOSALS.md`
**P-003** ("write the image-pipeline stages into `hearthlight-image-prompts`") before the proposal
was decided. The proposal called it *directly blocking*; whoever did the work agreed. P-003 should
be marked `done` at the workshop, not re-litigated.

`532c191` closes most of **P-005** (the UI correction loop) descriptively — the manual loop is
built; only automatic critique-to-prompt rewriting is left. That was named yesterday as the largest
gap between stated goal and built product, and it is now much smaller.

The compiler work answers `GOALS.md` core problem 2 (*style will not hold*) at the seam where it
actually fails: one prompt cell, one hash, one fingerprint, no assembled prose.

## North Star alignment

**ALIGNED** — the first day since the governance layer existed where the bulk of the work moved the
film rather than the system.

**Prior question** (*does it move Yu-Gi-Oh! toward finished, or harden the pipeline carrying it?*) —
yes, both. The Stage-A compiler and runner are the tooling `yugioh` needs to generate its stills.

1. **Fights drift.** Strongly. The prompt is the `Still (frame one)` cell verbatim; the compiler
   refuses anything else. Request fingerprints make "already generated" a fact, not a memory.
2. **Shifts the ratio toward direction.** Yes — two commands replace hand-typed prompts and per-shot
   Krea parameter fiddling. This is `GOALS.md` primary outcome 2 stated almost literally.
3. **Active use case today.** `yugioh`, this stage, this week.
4. **Seam test.** Improves it. Compile and run are separated, the runner cannot invent prompt text,
   and a crashed session resumes by job ID instead of re-spending.

**The flag, and it is bigger than yesterday's:** none of it is committed. 1.6 MB of the product's
most load-bearing new code — the paid-generation runner and the entire Studio UI — exists on one
disk, untracked. And the remote has **never received a single commit**: `origin/main` does not
resolve, so all **7** commits in the repository are unpushed, not the 6 the facts block reports.
Yesterday's top recommendation was a backup path; the exposure grew instead of shrinking. Alignment
is not the risk today. Loss is.

## Product-spec discrepancies

- **`PRODUCT_SPEC.md` §Features** describes Hearthlight Studio as working, but never says where it
  lives — and it lives in untracked `staging/overview-ui/`. The spec is not ahead of the code; it is
  ahead of **version control**. Either the Studio is product (commit it, give it a home in `skills/`
  or a named top-level dir) or it is scratch (say so in the spec). It cannot be both.
- **`PRODUCT_SPEC.md` §6 Known limitations** claimed 3 test files and "all 17 tests pass". There are
  now 5 files and 23 tests. Corrected descriptively.
- **`ROADMAP.md` §Where the code is** still lists `image_pass.py` and `two_pass.py` as the image
  front. Both Stage-A paths are now retired in favour of `krea_style_comp{,_run}.py`. Corrected.
- **`DECISIONS.md` has two D-015 entries** — cowork's *"The North Star aims at AI filmmaking"* and
  chatgpt's *"Krea still prompts are exact frame-one data"*, both dated 2026-08-03. The collision
  came from `b0570fd` sweeping in another agent's concurrent edit. Renumbering is Vince's call:
  rewriting an existing decision is off-limits to this run. **The second one should become D-016.**

## Complexity / orphan detection

- **`recovery/`** — 144 KB, 16 patch scripts (`2049.ps1`…`3139.patch`, `reconstruct_ui_patch.mjs`),
  untouched since 2026-08-03 17:37. A patch trail from a UI edit that has since landed in
  `staging/overview-ui/`. Almost certainly disposable; not deleted, and not gitignored either — so
  it will be committed by accident the first time anyone runs `git add -A`.
- **`staging/productions.js`** (80 KB, 17:46) is now superseded by
  `staging/overview-ui/productions.js` (82 KB, 22:02). Two copies of the same file, one stale.
- **`image_pass.py` / `two_pass.py`** — both Stage-A entry points are explicitly disabled in the
  skill text but still present, still carrying a test file (`test_image_pass.py`) whose failures
  now mean nothing. Retirement candidate for the workshop, not for this run.
- **Tests read live `projects/yugioh` data.** `test_krea_style_comp.py` asserts
  `generation_count == 28`, shared setups `{(5,1), (18B,17)}`, source-only `[29]`, and cell `H28`.
  Editing the film's shot list breaks the test suite. That is a genuine coupling defect: the
  checkpoint's commit guard can be tripped by a creative decision.
- Carried from yesterday, unchanged: `.agents/skills/krea-*` (~484 KB, zero references — **P-001**),
  `git-init-commit.sh` (the only file containing the leaked key literal), `HANDOFF.md` (**P-006**,
  and its skill count is still stale at 17 against 21).

## Unfinished work

**`yugioh` Stage-A generation is blocked right now.** `krea_style_comp.py --project yugioh --all
--check-only` returns a single line: *"Shot registry does not match current workbook; reconcile
before generation."* `warrior_returning_alive_shotlist-v4.xlsx` was modified 2026-08-04 01:22;
`shots.json` was last built 2026-08-03 19:31. The compiler is behaving correctly — the registry
needs rebuilding against v4 before any paid job runs. Nothing here touched `projects/`.

**Tests: 23 collected, 9 pass, 14 fail — neither cause is a code regression.**

- 6 failures in `test_krea_style_comp*.py` and `test_image_pass.py` are the registry drift above.
- `test_shot_registry.py` fails with `PermissionError: Operation not permitted: 'shot-specs.json'` —
  the agent sandbox cannot `rmtree` on the mounted Windows folder. Environmental.
- Committed with `--skip-tests`, cause read and recorded. Yesterday's "17/17 pass" claim is no
  longer reproducible and has been corrected in the spec rather than repeated.

Still open: **6 `⚠️ NEEDS VINCE` markers**, 3 TODO. `ROADMAP.md:49` — *"this list has not been
reviewed in four weeks"* — is now five weeks old and still unanswered. **6 proposals open** (5 AMBER,
1 GREEN), all raised yesterday, none decided; P-004 (ratify `yugioh/status.yml`) is GREEN and one
minute of work. `HANDOFF.md` claims 17 skills against 21 actual.

## Recommended next actions

1. **`git push origin main`, then commit the image pipeline and the Studio** — nothing has ever
   reached GitHub, and the paid runner, the compiler, both rewritten SKILLs and 1.4 MB of untracked
   UI are the only copy that exists.
2. **Rebuild `yugioh`'s shot registry against the v4 workbook** — Stage-A generation is blocked on
   it today, and v1 is at that stage.
3. **Renumber the duplicate `D-015` to `D-016`** — one edit, and it is the only decision-log
   ambiguity in the repository.

---

## Mechanical facts

<!-- Generated by governance/checkpoint.py. Do not hand-edit. -->

- Branch `main` · remote: https://github.com/vincetheeleventh/hearthlight.git
- Commits since baseline: **4**
- Files changed: **11**
- Uncommitted paths: **10**
- Unpushed commits: **6** ⚠ **nothing is backed up — push from Windows**
- By agent — chatgpt: 1, cowork: 3

<details><summary>Commits</summary>

- `532c1911` [chatgpt] docs: record interactive Hearthlight Studio state
- `957e3d00` [cowork] goals: North Star aims at AI filmmaking, not a competitor — propagated
- `b0570fdc` [cowork] governance: autonomous improvement loop + v1 definition
- `34e0a872` [cowork] docs: correct pytest limitation — 17/17 tests pass when installed

</details>

- Skills: **21**
- Unfinished markers: **9** (NEEDS VINCE×6, TODO×3)
- Proposals open: **6/7**
- ⚠ Stale skill counts: HANDOFF.md→17
- Tests: 5 file(s), not run (no runner)
- Secret scan: clean

*Facts: `checkpoints/.facts-2026-08-04.json`*
