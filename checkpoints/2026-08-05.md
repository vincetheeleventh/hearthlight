# Checkpoint — 2026-08-05

*Previous checkpoint: 2026-08-04*  
*Baseline commit: 57278ef048f5f5a804445c6b850c33137d5ed041*

---

## What changed?

**All three of yesterday's recommendations were executed.** The repository reached GitHub for the
first time — `origin/main` now resolves at `e3f57fd`.

**cowork** (5 commits, 11:17–12:30) — `745d38c` merged the remote char-sheet history, which is what
made the first push possible. `5b458be` committed the Stage-A compiler/runner split that had been
sitting untracked, and renumbered the duplicate `D-015` → `D-016`. `e3f57fd` put Hearthlight Studio
under version control in `staging/`. `586e841` landed **D-017**: canon documents state current state
only, history moves to `archive/`, and every canon doc carries YAML front matter — with
`governance/canon.py` (228 lines) to enforce it. `71c900a` set `yugioh`'s master aspect to 16:9 and
**parked P-007**, the directory restructure, on Vince's call, with the findings preserved in
`governance/audits/2026-08-04-yugioh-structure.md`.

**chatgpt** (3 commits — one attributed, two reported UNATTRIBUTED; see below) — `6d7333c` integrated
Shot Vision review and prompt authoring into Studio: `prompt_authoring.py` (869 lines),
`references/PROMPT-AUTHOR.md` (154), `test_prompt_authoring.py` (160), and decisions **D-018** and
**D-019**. `0e2546f` made current Shot Vision beat stale storyboard baseline everywhere it conflicts.
`79dee10` fixed the test that let retired shots 9–11 be reintroduced by a legacy batch.

**chatgpt, uncommitted (00:04–00:13 today), 10 files** — **D-020** supersedes D-019 after Vince read
a real Krea prompt. `render_prompt()` collapses to a pass-through: Python no longer appends the
aspect header, the locked style sentence, or the `Must show` annex. A new `PROMPT_SCAFFOLD_LEAK`
blocker catches those re-entering by hand, two author self-checks are added
(`relationally_coherent`, `non_redundant`), and PROMPT-AUTHOR.md gains a *relational prose before
atomic detail* section with a worked example. Consequently the **"No drift — copied verbatim" engine
law was reworded** in `AGENTS.md`, `PRODUCT_SPEC.md`, `hearthlight-shot-runner/SKILL.md`, and
`hearthlight-terse`'s NEVER-compress list.

## Why does it matter?

**`yugioh` Stage A is no longer blocked by the machine.** Yesterday the compiler stopped at *"Shot
registry does not match current workbook."* Today it stops at *"Shot 1 has no approved prompt for its
current Shot Vision revision."* The blocker moved from a hash mismatch to a Prompt Board approval —
from the system's fault to Vince's turn. That is `GOALS.md` Product principle 1 working as designed.

D-020 attacks `GOALS.md` core problem 2 — *style will not hold, and every model wants a different
prompt* — at the level the problem actually lives. Style now rides Krea's moodboard and strength
rather than a sentence competing with the description; the prompt carries one thing, the visible
frame. This is the **cross-platform prompt shaping** gap `ROADMAP.md` names, addressed for one
provider.

D-017 makes anti-drift **machine-checkable** for the first time at the doc layer: `canon.py check`
reports 9 documents clean. Every previous canon rule was advisory.

Parking P-007 is worth naming as a positive. The audit found a real structural problem and the
prioritization rule — *does it move the film, or harden the pipeline carrying it?* — parked it
anyway. The brake exists and it held.

## North Star alignment

**ALIGNED.** The heaviest development day since the governance layer existed, and nearly all of it
sits on the stage `GOALS.md` names as v1's current position.

**Prior question** — yes, both halves. Stage A moved from blocked to awaiting approval, and the
compiler/reviewer contract that carries it is now committed, tested, and pushed.

1. **Fights drift.** Strongly. D-017 plus `canon.py` turn "state current state only" from an
   instruction into a check. D-020 removes duplicated style/aspect layers, which were two places the
   same fact could diverge.
2. **Shifts the ratio toward direction.** Yes. Vince's only remaining Stage-A input is approval of
   the exact batch — no hand-typed prompts, no per-shot Krea fiddling.
3. **Active use case today.** `yugioh`, Shot 1, this hour.
4. **Seam test.** Improves the prompt seam, and adds seams: author → validator → reviewer → board.
   Each is instrumented, so this is acceptable — but **D-018 → D-019 → D-020 in 36 hours is three
   laws governing one seam**, and that rate is worth watching. Not drift; churn.

**The flag, and it needs Vince.** D-020 reworded an engine law across four files: *"style block +
signature blocks are copied verbatim into prompts"* became *"apply through the stage's declared
conditioning."* Sourced to Vince, so it is authorized. But `GOALS.md` Product principle 2 still reads
*"The locked style block, the character signature strings, the single aesthetic source of truth —
those **are** the deliverable,"* and that sentence now describes a law with a provider-shaped
exception carved into it. `GOALS.md` is RED and was not touched. Either the principle wants one
matching sentence from Vince, or the reworded law is narrower in practice than it reads on the page.

## Product-spec discrepancies

- **`DECISIONS.md` has no row for D-016, but D-016 is partly still in force.**
  `archive/decisions/D-018.md` states plainly: *"D-016's frame-one and no-motion protections remain,
  but its verbatim-cell rule is superseded."* The archived `D-016.md` reads `in_force: false`,
  `superseded_by: D-018`, and the rules table skips from D-015 to D-017. A rule that still binds and
  has no row is invisible to anyone reading the table, which the file itself calls *"the whole of what
  you need."* `canon.py check` passes — it validates front matter, not cross-references. Rewriting a
  decision is RED; this is surfaced, not fixed.
- **`PRODUCT_SPEC.md` §6** claims *"25 helper tests, including seven focused author-contract and
  visibility golden tests."* Unverifiable — `pytest` is still absent from this environment, so the
  number was neither confirmed nor corrected.
- **`HANDOFF.md` still claims 17 skills against 21** — third consecutive checkpoint. Its own §9 line
  admits the error. P-006 would move the file to `archive/` and dissolve the problem.
- Not a discrepancy but worth recording: `PRODUCT_SPEC.md` §2 and `ROADMAP.md` agree that Studio
  lives in `staging/overview-ui/` with its final home undecided. Yesterday's "spec ahead of version
  control" complaint is resolved — it is now tracked.

## Complexity / orphan detection

- **The `Agent:` trailer was defeated by escaping.** `0e2546f` and `79dee10` both end with the
  literal text `\n\nAgent: chatgpt` on one line rather than a real trailer, so the scan reported them
  UNATTRIBUTED. Two of the three unattributed commits are chatgpt's; only `f5ce737` (author
  `hearthlight`, arriving via the merge) is genuinely unlabelled. D-012 held — the changes were
  reported, not hidden — but the cause is a commit-message bug on the ChatGPT surface, not a missing
  trailer.
- **`recovery/` — third day flagged, unchanged.** 144 KB, 16 patch scripts, untouched since
  2026-08-03 17:37, still untracked and still not in `.gitignore`. The UI edit it was recovering has
  since landed and been committed. The next `git add -A` swallows it.
- **`staging/overview-ui/productions.js` and `productions.mjs` are both 1826 lines** and were
  committed together, alongside `staging/productions.js` (1530 lines, superseded). Three copies of one
  module now in history rather than one on disk.
- **`.agents/skills/krea-*`** — 484 KB across three packs, zero references. P-001, open 2 days.
- **`image_pass.py` / `two_pass.py`** — disabled in the skill text, still present, still carrying
  `test_image_pass.py`. Retirement is workshop business.
- **`yugioh/boy_char_sheet.png` and `father_char_sheet.png` are deleted in the working tree.** They
  entered through the merge from another machine as a root-level `yugioh/`, which contradicts D-006
  (`projects/` stays out of Git). The deletion looks correct and the blobs survive in history; it is
  simply not committed yet.

## Unfinished work

**`yugioh` Stage A waits on Vince, not on the machine.** `krea_style_comp.py --check-only` returns
*"Shot 1 has no approved prompt for its current Shot Vision revision."* That is the Prompt Board gate
doing its job before spend.

**Tests unverified.** `pytest` is absent, so all 7 test files report `not run`. The commit guard will
not block on failures it cannot observe — a limitation `PRODUCT_SPEC.md` §6 correctly states.

**5 unpushed commits**, all of today's chatgpt work plus three cowork commits, and **10 modified files
uncommitted** including the D-020 engine-law rewording. Better than yesterday's total exposure, but
the newest and least-reviewed work is the part that is not backed up.

Still open: **7 `⚠️ NEEDS VINCE` markers**, 3 TODO. **4 proposals open** (P-001, P-002, P-006 AMBER;
P-004 GREEN), all raised 2026-08-03, none decided; P-004 — ratify `yugioh/status.yml`, where gates 0–3
read `pending` while gate 4 is approved — is still one minute of work. `ROADMAP.md` open question 5,
the near-term list unreviewed for five weeks, remains unanswered; mentioned once, per the runbook.

## Recommended next actions

1. **Push, then commit the D-020 working tree** — 5 commits and 10 modified files, including a
   reworded engine law, exist only on one disk.
2. **Approve or correct Shot 1 on the Prompt Board** — the compiler says it is the only thing between
   `yugioh` and Stage-A generation.
3. **Give D-016 a row in `DECISIONS.md`, or declare it fully superseded** — D-018 says part of it
   still binds, and the rules table does not say so.

---

## Mechanical facts

<!-- Generated by governance/checkpoint.py. Do not hand-edit. -->

- Branch `main` · remote: https://github.com/vincetheeleventh/hearthlight.git
- Commits since baseline: **9**
- Files changed: **59**
- Uncommitted paths: **14**
- Unpushed commits: **5** ⚠ **nothing is backed up — push from Windows**
- By agent — UNATTRIBUTED: 3, chatgpt: 1, cowork: 5

<details><summary>Commits</summary>

- `79dee106` [unattributed] Test retired storyboard shots stay archived
- `0e2546f3` [unattributed] Fix Vision-first Krea prompt compilation
- `6d7333cf` [chatgpt] studio: integrate Shot Vision review and prompt authoring
- `71c900a4` [cowork] yugioh: master aspect is 16:9; park the restructure (P-007)
- `586e841b` [cowork] canon: current state only; history moves to archive/ (D-017)
- `e3f57fd4` [cowork] studio: track the Hearthlight Studio UI (staging/)
- `5b458bed` [cowork] image pipeline: Stage-A compiler/runner split, plus D-016 renumber
- `745d38c9` [cowork] merge: adopt the remote char-sheet commit (unrelated history)
- `f5ce7379` [unattributed] yugioh char sheets for krea reference

</details>

- Skills: **21**
- Unfinished markers: **10** (NEEDS VINCE×7, TODO×3)
- Proposals open: **4/7**
- ⚠ Stale skill counts: HANDOFF.md→17
- Tests: 7 file(s), not run (no runner)
- Secret scan: clean

*Facts: `checkpoints/.facts-2026-08-05.json`*
