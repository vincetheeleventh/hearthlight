# Checkpoint — 2026-08-06

*Previous checkpoint: 2026-08-05*  
*Baseline commit: 57278ef048f5f5a804445c6b850c33137d5ed041*

---

## What changed?

**Ten cowork commits, 12:17–20:42, in two arcs.** Nothing new from chatgpt or unattributed — the
four non-cowork commits in the facts below are yesterday's, carried by the rolling baseline.

**Morning — portability.** `ba388df` and `cde78e2` landed **D-021**: code moves by git, creative
work moves by Syncthing. The Story Studio root now resolves per-OS (`%LOCALAPPDATA%` /
`~/Library/Application Support` / `$XDG_DATA_HOME`, overridable by `HEARTHLIGHT_STUDIO_ROOT`);
previously the macOS fallback silently resolved to the repo directory. Ignore rules are committed at
`governance/syncthing/`. The measurement that decided it: 311 files / 79 MB of live creative work
against 15 files / 4.31 GB of immutable source media. 370 MB syncs, 5.6 GB stays.

**Evening — the video stage arrived.** `32600e9` imported an outside production practice —
`hearthlight-acting`, a `hearthlight-asset-sheets` skill, `prompt-architecture.md`,
`optics-language-bank.md`, `failure-locks.md`. `98778bc` catalogued `workflows/` with two routes
(**D-022**). `9b6799e` pointed the second route at the laws instead of restating them. `c2e9cf6`
**deleted `hearthlight-asset-sheets` twenty-one minutes after writing it** and distributed its
material to the skills that already owned those asset types (**D-023**, audit at
`governance/audits/2026-08-05-asset-sheets-boundary.md`). `61eee04` corrected the import: the
practice is photoreal-native, Hearthlight is painted.

`3395a3d` is the largest: **the medium law** (`medium: illustrated | live-action` in the
distribution spec; illustrated bans photographic vocabulary — no bokeh, focus plane, shutter blur,
pore-level skin, no `Photoreal` tag), the WF-A/WF-B → **shot2video / board2video** rename everywhere,
the new **`hearthlight-board-sheet`** skill, and the **generator swap**. `1a1f505` mapped the graph's
nodes and found `MiniMaxH3ReferenceToVideo` — closing the board2video surface gap four minutes after
flagging it. `b657755` added `set_shot_image.py`, a shot-image override for the dashboard.

**The film moved too** (untracked, per D-006): the mother character sheet was built at 19:24,
`shot-02-v02.png` was set as hero through the new script at 20:42 with the reject-list note *"no
tags, no object, none of her hands"*, video prompt packets were written for shots 2 and 8, and the
MiniMax graph was copied into `yugioh/workflow-development/comfy_graphs/`.

## Why does it matter?

**The generator changed, and it is the one that carries v1's next phase.** Stage 6 was RunningHub
Seedance — remote, metered, and carrying a leaked API key this checkpoint has flagged for three days.
It is now local ComfyUI `minimax_h3_i2v_int8`, confirmed working by Vince on `yugioh` Shot 1. That
removes a cost gate and a credential liability from the exact stage `ROADMAP.md` names as **Next**,
and it is `GOALS.md` core problem 5 behaving as designed: the ground moved, a workflow was retired
rather than remembered.

**The medium law is the sharpest anti-drift move in weeks.** Core problem 2 — *style will not hold* —
was previously fought one prompt at a time. `medium: illustrated` now bans an entire vocabulary at
the spec layer, and the photoreal material is parked in `references/live-action/` behind a README
saying why it must not be opened for an illustrated film. A category of drift is closed rather than
caught.

**D-023 is product principle 3 running in the machine.** Vince asked whether the separation was
necessary; the agent audited its own work an hour old, found four laws now stated twice, and deleted
the skill. *"A partner, not an intern. Compliance is a bug."* The entry also leaves the caveat for
the next agent who finds those rules scattered and wants to gather them back up.

**`workflows/` answers core problem 4 directly** — *workflows live in one person's head*. Two routes,
a selection guide written **before** the trial so the trial can falsify it, and a comparison ledger
that settles the question on attempts-to-approval rather than on whichever route was tried most
recently.

## North Star alignment

**ALIGNED** — with one cost worth naming.

**Prior question — yes, both halves.** The film moved (mother sheet, shot-02 hero selection, two
video prompt packets) and the pipeline stage it is about to enter got a working, local, unmetered
generator. This is the rare day where "move the film" and "harden the pipeline" were the same work.

1. **Fights drift — strongly.** The medium law removes a vocabulary. D-023 removed four duplicated
   laws and verified each now appears in exactly one file. The workflow rename removed two opaque
   labels. Every one of these reduces the number of places a fact can be stated.
2. **Shifts the ratio toward direction — yes.** `set_shot_image.py` takes a manual override off
   Vince's plate. `hearthlight-board-sheet`'s whole thesis is that the prompt afterwards is one
   sentence. A local generator removes credit arithmetic from every experiment.
3. **Active use case today — yes, all of it.** Every item ran against `yugioh` on 2026-08-05.
   `hearthlight-board-sheet` is the one exception and `SKILL-INVENTORY.md` correctly labels it
   **EXPERIMENTAL**, which is exactly what criterion 3 asks for.
4. **Seam test — a new route is a new seam,** and D-022 attaches a ledger to it rather than an
   opinion. Acceptable. `hearthlight-acting` is genuinely cross-cutting and adds no handoff.

**The cost: import velocity outran vetting.** Two of the day's ten commits exist only to correct
material committed 20–35 minutes earlier by the same author — `c2e9cf6` deleting a skill it had just
written, `61eee04` fixing a photoreal assumption baked into the imported text. Both corrections were
right and both were caught the same hour. But the outside practice arrived as a bulk import and was
vetted *after* landing. Yesterday's checkpoint flagged three laws over one seam in 36 hours as
*churn, not drift*; this is the same pattern in a different place. It cost two commits on a day Vince
was reading. On a day he is not, it costs a law.

## Product-spec discrepancies

- **`AGENTS.md` § pipeline stage 8 read "Seedance i2v via RunningHub"** while
  `hearthlight-comfyui-graph` states the active generator is local MiniMax H3 and RunningHub is
  parked. An agent following the index would have gone to the parked provider. **Corrected
  mechanically this run.**
- **`AGENTS.md` omitted `hearthlight-acting` and `hearthlight-board-sheet`** from the pipeline index —
  both flagged by `gather`. **Added.**
- **`PRODUCT_SPEC.md` §3 row 6** said *"Seedance i2v clips"*, and **§6** said *"The RunningHub key was
  leaked and needs rotating; no full run"* as if it still blocked Stage 6. **Corrected descriptively**
  — the key still wants rotating, but it is no longer on the critical path. §3 also gained
  `hearthlight-board-sheet`.
- **`ROADMAP.md` had not been touched since 2026-08-04** while the heaviest change day since the
  governance layer existed landed on top of it: no generator swap, no workflow trial, no medium law.
  **Updated.**
- **`SKILL-INVENTORY.md` line 116 said "22 skills"** against 23. **Corrected.**
- **`archive/decisions/D-020.md` has never been in git.** `DECISIONS.md` has carried the D-020 row
  since yesterday, and `D-019.md`'s supersession (`in_force: false`, `superseded_by: D-020`) exists
  only in the working tree. A law in force whose argument file is untracked. `canon.py check` passes
  because it validates the front matter of files it finds, not the existence of files a table
  references. **This run's commit sweeps `archive/`, which closes it** — but the gap is worth naming,
  because the check that should have caught it cannot.
- **`DECISIONS.md` still has no row for D-016** — third consecutive checkpoint. `D-018.md` states its
  frame-one and no-motion protections remain in force. Rewriting a decision is RED; still surfaced,
  still unfixed.
- **`HANDOFF.md` still claims 17 skills** against 23 — fourth consecutive checkpoint. P-006.

## Complexity / orphan detection

- **`skills/hearthlight-video-prompts/references/live-action/` — new, and speculative by its own
  admission.** 11 KB of optics language for a medium no current project uses, inside a **CORE**
  skill. It is parked correctly and the README explains why it must not be opened. But `GOALS.md`
  criterion 3 says speculative material must be *labelled EXPERIMENTAL in `SKILL-INVENTORY.md`, not
  quietly absorbed* — and a reference subtree inside a core skill is not visible to that inventory at
  all. Workshop business, not a fix.
- **`recovery/` — fourth day flagged, unchanged.** 144 KB, 16 patch scripts, untracked, still not in
  `.gitignore`. The next `git add -A` swallows it.
- **`yugioh/boy_char_sheet.png` and `father_char_sheet.png` still show as deleted-uncommitted** —
  second day. They arrived through the merge as a root-level `yugioh/`, contradicting D-006. The
  deletion is correct; it simply has not been committed, and the checkpoint's safe-stage list does
  not reach it.
- **Skills are getting large.** Five `SKILL.md` files now exceed 12 KB — `mise-en-scene` 15.9 KB,
  `image-prompts` 14.9 KB, `conventions` 13.9 KB. D-023 chose distribution over splitting on
  purpose, and that choice pushes material *into* these files. Consistent, not contradictory, but the
  trend is now the direct consequence of a law and should be watched rather than acted on.
- **Unchanged from previous checkpoints:** `.agents/skills/krea-*` (3 packs, 484 KB, zero references,
  P-001 open 3 days) · `staging/overview-ui/productions.js` and `.mjs` both 1826 lines ·
  `image_pass.py` / `two_pass.py` disabled but present with a test file · `hearthlight-timing-intake`
  4 scripts / 0 tests.

## Unfinished work

**8 unpushed commits — nothing is backed up.** Two days running. Everything above exists on one disk.

**`yugioh/status.yml` is now actively misleading, not merely stale.** Gates 0–3 read `pending` while
gate 4 is approved; video prompt packets for shots 2 and 8 were written last night against a ledger
that says images were never gated. P-004 is GREEN, one minute of work, and open 3 days.

**Tests still unrun** — `pytest` absent, 7 files, so the commit guard cannot block on a failure it
cannot observe.

**5 proposals open**, P-011 (headless front figure on the character sheet) raised yesterday and
sitting in front of a sheet that is `image2` on every video job. **7 `⚠️ NEEDS VINCE` markers**; the 3
TODO hits are the marker scanner matching its own regex — noise, not work.

`ROADMAP.md` open question 5 — the near-term list unreviewed for six weeks — mentioned once, per the
runbook.

## Recommended next actions

1. **Push.** 8 commits including the generator swap, three decisions, and two new skills exist only
   on one machine.
2. **Ratify `yugioh/status.yml` (P-004)** — one minute, and it is no longer cosmetic: video work is
   being written against a ledger claiming the image gate never happened.
3. **Log the first row in the `workflows/README.md` comparison ledger.** The trial is built and the
   table is empty; every day it stays empty is a day the shot2video/board2video answer comes from
   memory instead of evidence — the exact failure D-022 was written to prevent.

---

## Mechanical facts

<!-- Generated by governance/checkpoint.py. Do not hand-edit. -->

- Branch `main` · remote: https://github.com/vincetheeleventh/hearthlight.git
- Commits since baseline: **19**
- Files changed: **86**
- Uncommitted paths: **7**
- Unpushed commits: **8** ⚠ **nothing is backed up — push from Windows**
- By agent — UNATTRIBUTED: 3, chatgpt: 1, cowork: 15

<details><summary>Commits</summary>

- `b6577558` [cowork] dashboard: set_shot_image.py — override a shot's chosen image
- `1a1f505c` [cowork] graph: node map for MiniMax H3, and the board2video surface exists
- `3395a3d0` [cowork] medium law, descriptive workflow names, MiniMax generator, board sheet
- `61eee042` [cowork] fix: the imported practice is photoreal-native, Hearthlight is painted
- `9b6799e9` [cowork] workflows: WF-B pointed at the laws instead of restating them
- `c2e9cf6c` [cowork] audit: delete hearthlight-asset-sheets, distribute to real owners
- `32600e9c` [cowork] skills: asset factory + full prompt-director depth
- `98778bc5` [cowork] workflows: catalogue two routes from storyboard to clip
- `cde78e20` [cowork] port: canonical Syncthing ignore rules under governance/
- `ba388dfb` [cowork] port: macOS support — dashboard launcher + Syncthing rules
- `79dee106` [unattributed] Test retired storyboard shots stay archived
- `0e2546f3` [unattributed] Fix Vision-first Krea prompt compilation
- `6d7333cf` [chatgpt] studio: integrate Shot Vision review and prompt authoring
- `71c900a4` [cowork] yugioh: master aspect is 16:9; park the restructure (P-007)
- `586e841b` [cowork] canon: current state only; history moves to archive/ (D-017)
- `e3f57fd4` [cowork] studio: track the Hearthlight Studio UI (staging/)
- `5b458bed` [cowork] image pipeline: Stage-A compiler/runner split, plus D-016 renumber
- `745d38c9` [cowork] merge: adopt the remote char-sheet commit (unrelated history)
- `f5ce7379` [unattributed] yugioh char sheets for krea reference

</details>

- Skills: **23**
- ⚠ Absent from `AGENTS.md`: hearthlight-acting, hearthlight-board-sheet
- Unfinished markers: **10** (NEEDS VINCE×7, TODO×3)
- Proposals open: **5/7**
- ⚠ Stale skill counts: SKILL-INVENTORY.md→22, HANDOFF.md→17
- Tests: 7 file(s), not run (no runner)
- Secret scan: clean

*Facts: `checkpoints/.facts-2026-08-06.json`*
