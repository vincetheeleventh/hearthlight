# Checkpoint — 2026-08-03

*Previous checkpoint: none (first run)*  
*Baseline commit: repository start*

---

## What changed?

**cowork** — Built the governance layer and, in the process, gave Hearthlight version control for the
first time.

- **Git initialized.** No repository had ever existed. `git-init-commit.sh` (written 2026-07-01)
  could never succeed: its leaked-key guard grepped for a key literal written *inside the guard
  itself*, so it self-aborted on every run. First commit `2226721b` tracks 158 files.
- **Canon established** — `GOALS.md`, `PRODUCT_SPEC.md`, `ROADMAP.md`, `DECISIONS.md` (12 entries),
  `SKILL-INVENTORY.md` (21 skills classified).
- **Checkpoint engine** — `governance/checkpoint.py`, stdlib only, split `gather` / `commit`.
- **Multi-agent coordination** — commit `Agent:` trailers, unattributed-change detection, working
  agreement in `AGENTS.md`.
- **Stale docs corrected** — `README.md` was missing 7 of 21 skills and described a deleted
  `hearthlight-asset-bible/` tombstone; `AGENTS.md` never mentioned `hearthlight-dashboard`;
  `HANDOFF.md` marked as a superseded historical snapshot.
- **Miro** — governance panel added beside the pipeline diagram. The diagram itself untouched.

**UNATTRIBUTED** — Another agent is working in this folder **right now.** `staging/productions.js`
(72 KB) and `recovery/*.ps1|*.js` (12 patch scripts) appeared at 17:15–17:17 during this session.
Content is dashboard work: shot-structure dialogs, `reviewAssetForShot` asset-stage precedence.
Left untouched and uncommitted.

## Why does it matter?

The disorientation that prompted this system had a concrete cause: **a month of development with no
version history.** Nothing recorded who changed what, when, or why — across three agents. Every
symptom Vince described (can't tell what's built, why it exists, whether it's still needed) follows
from that single absence.

Connects to `ROADMAP.md` known problems 1–5, all of which were discovered rather than assumed. The
`staging/` find is `DECISIONS.md` D-012 demonstrating itself within an hour of being written: two
agents in the same folder, no shared memory, invisible to each other except through files.

## North Star alignment

**NEUTRAL / INFRASTRUCTURAL** — with one flag.

Against the six criteria in `GOALS.md`: passes 1 (protects the pen — the automation explicitly cannot
touch `GOALS.md`, delete anything, or refactor), 2 (engine-level, no client assumptions), 3 (fights
drift — that is its whole function), and 6 (the seam test — attribution makes agent handoffs visible
for the first time).

Criterion 4 (**shifts the ratio toward direction**) is where honesty is required: this adds a daily
artifact Vince must read. It only pays if the checkpoints surface things he would otherwise miss.
Judge it in two weeks — if they read as noise, cut the cadence to weekly.

**The flag:** governance is not filmmaking. Today produced zero progress on the pilot. That is
defensible once. Repeated, it is the drift pattern to watch — the system improving the system while
`mcconaughey-call` sits at seven `unconfirmed` gates.

## Product-spec discrepancies

`PRODUCT_SPEC.md` was written today against the live tree, so it is current by construction. Two
discrepancies were found in the *pre-existing* docs and fixed:

- `README.md` § Layout — omitted `hearthlight-character`, `-critique`, `-dashboard`, `-notion-log`,
  `-selfcheck`, `-shot-crew`, `-timing-intake`. Fixed.
- `AGENTS.md` § pipeline — `hearthlight-dashboard` absent entirely despite owning the Shot ID
  protocol (D-009). Added.

**Open discrepancy:** the Miro pipeline diagram does not show `hearthlight-character`,
`-dashboard`, or `-shot-runner`. Deliberately not auto-fixed — it is hand-built artwork and a
nightly bot mutating 52 hand-placed items will eventually corrupt it. Vince's call.

## Complexity / orphan detection

- **`.agents/skills/krea-{animation,generate,marketing}`** — ~484 KB, 18+ Python scripts, **zero
  references anywhere in the repository.** `krea-animation` is a complete parallel pipeline
  (`assemble_edit.py`, `submit_video_jobs.py`, `scaffold_project.py`) overlapping
  `hearthlight-storyboard`, `-shot-runner`, `-mise-en-scene`, `-conventions`. Highest-value cleanup
  available. Not deleted — surfaced.
- **`hearthlight-shot-crew`** — EXPERIMENTAL. Most expensive component per shot; its core premise
  (eight roles produce genuinely distinct opinions) is named in `HANDOFF.md` §6 as still unverified.
- **`hearthlight-dashboard`** — UNCLEAR. Largest sub-app, most recent work, invisible to the
  operating index until today.
- **`hearthlight-character`** — the *opposite* problem: owns the turnaround law inherited by every
  generated clip, but has zero peer references. Under-integrated for its blast radius.
- **`.artifact-work/`** — 944 KB of one-off scratch including the repo's only `node_modules`. Now
  gitignored.
- **The engine/client abstraction** — built for many clients, exactly one exists. Justified only if
  more are coming; see `GOALS.md` open question 3.

## Unfinished work

- **11 unfinished markers** — 8 `⚠️ NEEDS VINCE` (4 in `GOALS.md`, the rest in `ROADMAP.md`), 3 TODO.
- **Tests cannot run.** 3 test files exist; `pytest` is not installed in the agent sandbox, so
  `checkpoint.py commit` cannot verify them. It reports `not run` rather than falsely passing.
- **`HANDOFF.md` §5 open items** — 11 items, unreviewed for four weeks. At least three verifiably
  not done: the `gemma → LocalHermes` rename, the STT venv rebuild, the `TASTE.md` "love list".
- **`.venv-stt` dead** — blocks `hearthlight-timing-intake/scripts/transcribe.py`.
- **RunningHub key** — leaked, rotation status unknown. Blocks Stage 6.
- **In-flight, another agent** — `staging/productions.js` mid-edit with a `recovery/` patch trail.
- **No git remote configured** — commits are local only.

## Recommended next actions

1. **Give me the GitHub remote URL** — until then the entire history lives on one machine with no
   backup, which is the same fragility that caused this problem.
2. **Answer `GOALS.md` open question 2** (*what does "done" look like for v1?*) — no checkpoint can
   honestly judge whether work is converging without a success criterion; it is one sentence.
3. **Decide on `.agents/skills/krea-*`** — keep, harvest, or delete. It is the largest block of
   unexplained complexity in the repository and one word from you resolves it.

---

## Mechanical facts

<!-- Generated by governance/checkpoint.py. Do not hand-edit. -->

- Branch `main` · remote: **none configured**
- Commits since baseline: **1**
- Files changed: **0**
- Uncommitted paths: **4**
- By agent — cowork: 1

<details><summary>Commits</summary>

- `2226721b` [cowork] Hearthlight instruction layer — first tracked state

</details>

- Skills: **21**
- Unfinished markers: **11** (NEEDS VINCE×8, TODO×3)
- Tests: 3 file(s), not run (no runner)
- Secret scan: clean

*Facts: `checkpoints/.facts-2026-08-03.json`*
