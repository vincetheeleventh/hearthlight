---
doc: PROPOSALS
role: backlog
authority: canon
owner: agents
updated: 2026-08-04
answers:
  - what improvements are awaiting a decision
  - which v1 blocker each would remove
  - what an agent may execute without asking
not_here:
  decided and rejected proposals: archive/proposals.md
  what is being built now: ROADMAP.md
archive: archive/proposals.md
---

# PROPOSALS — the improvement backlog

*Where the system's own ideas for improving itself live. Generated by the weekly workshop, decided
by Vince.*

---

## How this works

**Every proposal must name the v1 blocker it removes.** v1 is the finished Yu-Gi-Oh! film plus the
architecture that finished it (`GOALS.md`). A proposal that cannot name what it unblocks is **parked**,
not built — however clever it is. This is the sharpest anti-drift tool in the system.

**Removal proposals are first-class.** `GOALS.md` core problem 5 makes retiring obsolete workflows a
*goal*: new models make old skills worthless, and a system that only accumulates cannot keep pace.
A proposal to delete or merge something is worth more than one that adds — it still needs Vince's ✅
(deletion is RED), but it does not need to justify itself as harder than an addition would.

### The three brakes

An agent asked to improve things will always find things to improve. Without limits this backlog
becomes the bloat it exists to prevent.

| Brake | Rule |
|---|---|
| **WIP limit** | Max **7** open at once. A new idea must **displace a worse one**, not just be appended. Forces ranking. |
| **Expiry** | Open **30 days** with no decision → the workshop expires it. If it mattered, it comes back. |
| **The v1 test** | No named v1 blocker → `parked`. Reconsidered when v1 ships. |

### Autonomy tiers (`DECISIONS.md` D-013)

| Tier | Meaning | Agent may |
|---|---|---|
| **GREEN** | Mechanical, reversible, no product judgment — doc/index sync, dead refs, stale counts, tests for existing code, Miro refresh, gitignore hygiene | **Execute, commit, report after.** No approval needed. |
| **AMBER** | Changes how the system behaves — new skill, changed skill contract, merging or retiring a component, restructuring, new dependency | **Propose only.** Waits for Vince's ✅. |
| **RED** | `GOALS.md` · the gate protocol · deleting anything · product strategy · charged register · `profile/TASTE.md` | **Never autonomous. Ever.** Proposal only, permanently. |

### Status values

`open` (awaiting decision) · `accepted` (✅, not built) · `done` · `rejected` · `parked` (no v1 link
yet) · `expired`

**Rejected and expired rows are never deleted** — same principle as the ideation boneyard. A rejected
idea that keeps resurfacing is information.

---

## Open

| ID | Raised | Tier | Status | Proposal | v1 blocker it removes |
|---|---|---|---|---|---|
| P-001 | 2026-08-03 | AMBER | open | **Decide the fate of `.agents/skills/krea-*`** — 3 vendored packs, ~484 KB, 18+ scripts, zero references. Harvest `krea-generate/references/seedance-2.md` and `model-catalog.md` if useful, retire the rest. | Every agent reading the tree must work out whether these are live. Ambiguity here costs attention on every session touching image/video generation — the exact stage v1 is at now. |
| P-002 | 2026-08-03 | AMBER | open | **Create `skills/hearthlight/SKILL.md`** — the router exists only in the Claude skill store with no canonical source, the one place D-002 is not followed. Make the store copy a stub like every other. | The session entry point can drift with nothing to reconcile against. It is the first file every agent reads. |
| P-004 | 2026-08-03 | GREEN | open | **Ratify `yugioh/status.yml`** — gates 0–3 read `pending` while gate 4 is `approved 2026-07-30`. The film outran its ledger. | The gate ledger is the only machine-readable record of v1 progress. Wrong, it makes every alignment judgment unreliable. |
| P-005 | 2026-08-03 | AMBER | open | **Close the correction loop in Studio** — a per-shot "what's wrong" capture that writes a structured revision note the image and video prompt skills consume, so a critique becomes a proposed prompt revision instead of a chat message. | **`GOALS.md` core problem 1 — the largest gap between stated goal and built product.** The loop the image pipeline is hand-run through on every shot. |
| P-006 | 2026-08-03 | AMBER | open | **Move `HANDOFF.md` to `archive/`.** Its §1 frames the problem commercially and states Talefeather's grief reasoning as engine law. | A fresh agent reading it inherits the wrong North Star and the grief-cohort leak (D-003). Costs attention on every session, including every checkpoint. |

## Decided

| ID | Raised | Tier | Status | Proposal | Outcome |
|---|---|---|---|---|---|
| — | — | — | — | *(nothing decided yet)* | — |

---

## Writing a good proposal

- **One change per row.** If it needs "and", it is two proposals.
- **Name the blocker concretely.** "Improves quality" is not a blocker. "Style composition stage is
  undocumented, so it breaks when a fresh session runs it" is.
- **Say what you would do, not what is wrong.** Findings belong in the checkpoint; proposals are
  actions.
- **Prefer deletion and merging over addition.** The best proposal removes something. A backlog that
  only ever grows the system has misunderstood the job.
